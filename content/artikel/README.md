Diese HTML-Dateien sind interne Source-Artefakte fuer die servergerenderten Artikelseiten.

Sie bleiben fuer Redaktion, Diffing und Tests im Repository, werden aber nicht mehr direkt aus
dem `public/`-Verzeichnis ausgeliefert.
